import React from 'react';
import Reflux from 'reflux';

/**
 * Actions for message list
 */
const MessageListActions = Reflux.createActions([
    'loadMessages'
]);

export default MessageListActions;